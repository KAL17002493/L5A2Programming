@*Update*@
@model L5A2Programming.Models.RoomViewModel
@{
}
<h1>Update</h1>


<div class="container">
    <form method="post" asp-action="Edit" enctype="multipart/form-data">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="">
            <input type="hidden" asp-for="Room.Id" />
            @*Displays an empty input box*@
            <div class="mb-3">
                <label for="Asset.AssetName" class="form-label">Room Name</label>
                <input asp-for="Room.Name" type="text" class="form-control" id="Room.Name" placeholder="Room Lable" name="Room.Name">
                <span asp-validation-for="Room.Name" class="text-danger"></span>
            </div>
            @*Displays a doropdown list of all insitutions*@
            <div class="mb-3">
                 <label for="" class="form-label">Institution</label>
                @Html.DropDownListFor(m => m.Room.InstitutionId, Model.Institution, "Select an Institution", new {@class = "form-select"})
                <span asp-validation-for="Room.InstitutionId" class="text-danger"></span>
            </div>


 <div class="container">
    <div class="row">

        <div class="col">
            <div class="d-grid gap-2">
                    <a class="btn btn-secondary" asp-action="Index">Back to List</a>
        </div>
        </div>
        <div class="col">
            <div class="d-grid gap-2">
            <button class="btn btn-success" type="submit">Update Room</button>
            </div>
        </div>

     </div>
 </div>

        </div>
    </form>
</div>